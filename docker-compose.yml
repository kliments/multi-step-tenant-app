version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    volumes:
      - ./frontend:/app
    networks:
      - app-network
    command: ["nginx", "-g", "daemon off;"]

networks:
  app-network:
    driver: bridge
